# INDEX — artifacts/04-implementation

本目录由 `subagents/04-implementation` 唯一写入，网站位于 `site/`。

## 关于 site/ 目录的索引豁免

`site/` 是交付产物目录，契约要求**恰好 3 个文件**（index.html、styles.css、site-spec.json），
因此该子目录**不放置 INDEX.md**；其说明由本文件承担。验证实现时不得因缺少 site/INDEX.md 判定失败。
